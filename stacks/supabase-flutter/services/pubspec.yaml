name: {{ project_slug }}_services
description: "Dart service layer for {{ project_name }} — domain logic + API clients."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
