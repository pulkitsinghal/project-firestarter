/// {{ project_name }} service layer (skeleton).
///
/// This package owns domain logic and API access. Keep the canonical write
/// paths here (never re-implement them in the Flutter widget tree). No bare
/// `Exception` — define a sealed `PilgrimException` hierarchy and throw those.
library;

String greeting() => 'hello from {{ project_slug }}';
