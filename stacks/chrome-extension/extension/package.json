{
  "name": "{{ project_slug }}-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.15.0",
    "esbuild": "^0.28.1",
    "rimraf": "^6.1.3",
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  }
}
