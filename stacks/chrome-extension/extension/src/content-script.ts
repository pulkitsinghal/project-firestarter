// {{ project_name }} — content script. Runs on the pages matched in
// manifest.json's content_scripts. Kept trivial; grow it to read or annotate the
// page (and add host_permissions if you need cross-origin access).
console.debug('{{ project_slug }} content script active on', location.href);
