# Lift Log — harvesting learnings back into the template

This template is a living distillation, not a one-time snapshot. When a sibling
project discovers something worth standardizing, lift it back here so the *next*
project starts with it.

## When to lift

Lift something when it's **process or infrastructure that every sibling would
want**, not project-specific behaviour. Good candidates:

- A CI workflow fix (a race, a permissions bug, a better cache key).
- A new git hook or a hardening of an existing one.
- A documented gotcha (an image pin, a startup-ordering quirk, an SDK flag).
- A better `make` target or a clearer doc section.

Do **not** lift: domain logic, product copy, anything tied to one project's
schema or features.

## How to lift

1. Generalize it: replace project-specific names with `{{ tokens }}`.
2. Decide where it belongs:
   - Universal? → `template/`.
   - Stack-specific? → the relevant `stacks/<stack>/`.
3. If it's a gotcha, bake it as an **inline comment** next to the code it guards,
   and add a row to `docs/ANATOMY.md`.
4. Add an entry to the log below.
5. Re-stamp a throwaway project (`./bin/firestart.sh --defaults --output /tmp/smoke`)
   and confirm it still generates cleanly.

## Periodic sweep

Every so often (e.g. when starting a new project), skim the siblings'
`PROJECT_STATUS_AND_NEXT_STEPS.md`, recent CI changes, and any new
`docs/postmortems/` for things worth lifting.

## Log

| Date | Lifted | From | Into |
|------|--------|------|------|
| 2026-07-30 | **Generic engineering conventions** (`template/docs/ENGINEERING_CONVENTIONS.md` + AGENTS/CLAUDE pointers): distilled four vendor-neutral operating directives every project wants. (1) The authoritative quality gate = code review + the test pyramid run in order (unit→integration→api→e2e) locally at high intensity in Docker; a green local gate is sufficient to merge, stays authoritative when the hosted CI run is unavailable/flaky for unrelated reasons, and is a code-quality gate only — never a licence for owner-gated side-effects. (2) Stacked-PR merge order: merging a base PR with `--delete-branch` auto-closes a child PR based on it → retarget the child onto the default branch first, plus the `git rebase --onto` recovery. (3) Fork discrete self-contained work into its own branch/PR to keep diffs small and independently mergeable. (4) Ask for decisions visually (storyboard/annotated narrated cut, question→motivation→proposal→ask), not a wall of text. Prose is fully generic — no product/owner/domain specifics. | distilled cross-project engineering practice | template/docs/ENGINEERING_CONVENTIONS.md + template/AGENTS.md + template/CLAUDE.md + docs/{ANATOMY,LIFT-LOG}.md |
| 2026-07-29 | **Semantic-first browser automation policy 0.1**: converted the paused public-source research checkpoint into a default-off, source-only Firestarter add-on rather than lifting a live browser implementation. The closed contracts bind task/tab/document/frame/origin/generations and exact adapter/action/effect/full-target digests, enforce pre/postconditions, confirmation tiers, reversible draft plans, atomic in-process replay state, prompt-injection isolation, and content-minimized evidence. Semantic DOM resolution is authoritative; Relative XY is a trained, proposal/handoff-bound fallback that cannot bypass ambiguity, overlays, stale state, closed shadow roots, or cross-origin denial. Citrix/remote and voice intent remain separate adapters. Every rich synthetic fixture executes its declared outcome across lifecycle replacement, frames, shadow DOM, hidden/duplicate/overlaid/dynamic elements, malicious page-instruction signals, replay/collision/indeterminate outcomes, navigation races, and geometry/session drift. Handoffs bind nonce and idempotency for a separately reviewed durable consumer. The add-on contains no extension, browser/profile/session access, input injection, screenshot/OCR, credential, network, executor, or deployment. | `browser-automation-best-practices/outputs/browser-automation-best-practices-checkpoint.md` (2026-07-28 public-source checkpoint, explicitly treated as untrusted design input) | `addons/browser_automation_policy/` + `tests/test_browser_automation_policy_contract.py` + `bin/generate.py` + `firestarter.config.json` + `.github/workflows/ci.yml` |
| 2026-07-28 | **Observe-only local service supervisor contract**: added an opt-in, stack-agnostic `service_supervisor` add-on that owns the strict service catalog, dependency/lifecycle state machine, deterministic wake/idle planner, lease/grace and pin policy, bounded readiness, coalesced first wake, rollback/crash cleanup, truthful unknown metrics, and content-free telemetry. The first adapter is synthetic only; exact unknown fields, unsafe adapters/commands, external or ambiguous bindings, duplicate IDs/upstreams, unknown dependencies, and cycles fail closed. The local-ai schema mapping preserves every unavailable runtime fact and never treats `managed:false` as execution permission. A GET/HEAD wake-before-forward broker is test-only; runtime source has no listener, data-plane proxy, Docker socket, subprocess/OS executor, install path, or host-state mutation. Real typed adapters and the later Caddy wake broker remain separately gated. | read-only Mac service architecture, resource audit, and local-ai observe-only inventory contract | `addons/service_supervisor/` + `bin/generate.py` + `firestarter.config.json` + `.github/workflows/ci.yml` |
| 2026-07-28 | **Canonical Orchestrator Bill of Rights and executable local control plane**: consolidated durable portfolio-delivery policy into one generic human authority, then encoded stable scoped rules behind a prompt-safe Python-stdlib/SQLite interface. `prepare-launch` transactionally deduplicates transport-independent source event, outcome, idempotency, and canonical repo/path ownership before external creation; fences and rule-echo receipts stop stale/untrained mutation; `classify-decision` distinguishes routine PM proxy from bounded typed owner gates and keeps zero-step billing CI informational; `record-handback` starts crash-recoverable predecessor archive and reserved successor outboxes with typed evidence/cleanup; `recycle-queue` re-audits blocked work before replenishment. Local `0700`/`0600` state stores no raw prompt/hash, legacy free-text boards remain quarantined display data, dashboards allow only HTTP(S) links under CSP, and rules cannot execute commands. Added JSON schemas, deterministic exits, effective-rule reasoning, a Phase-2 Codex wrapper contract, 100-repeat process race coverage, crash/fencing/precedence/migration/privacy adversarial tests, and every-stack stamp contracts. | durable cross-project orchestrator decisions, observed duplicate-lane failures, and adversarial architecture review | `addons/orchestrator_session/` + `tests/test_orchestrator_{control,session_contract}.py` + `.github/workflows/ci.yml` + `docs/{ANATOMY,LIFT-LOG}.md` |
| 2026-07-28 | **Phase-2 PM-proxy plugin and atomic closure/refill**: packaged the Firestarter interface as a repo-local source plugin with validated prompt-free receipts and task-tool stubs. Schema 1.1 now records capacity release in the handback transaction, normalizes clean `interrupted/notLoaded`, fences archive on exact successor receipt, exposes receipt/handback-derived deficit truth, and provides event-driven plus watchdog reconciliation. The exact missed-closeout regression and 100 repeated concurrent closure/refill races prove one successor without owner prompting. | Phase-2 integration contract, independent race/privacy review, and observed missed closeout | `addons/orchestrator_session/common/{.agents,plugins,orchestrator-control}/` + `tests/test_orchestrator_{control,session_contract}.py` |
| 2026-07-28 | **Mandatory root-orchestrator role guard**: added an allowlist-only pre-action classifier as a control-plane invariant, not prompt etiquette, so root remains coordination-only while eligible worker capacity exists and delegates task-domain inspection, design, coding, testing, estimation, deployment, and cleanup. Missed delegation cannot authorize direct work. The sole exception is an exact action/scope-bound `ROOT_EXECUTION_EXCEPTION` for `SYSTEM_NONDELEGABLE_RECOVERY`, zero eligible workers, and a ≤300-second lifetime; stale, mismatched, broader, or worker-eligible receipts fail closed. Exact prepare/launch receipts gate visible launch and capacity truth; receipt/heartbeat/worker-handback evidence gates `assigned`, `running`, `validated`, `merged`, and `deployed`. The bounded audit retains only redacted classification metadata and fails closed on unknown actions, private fields, or corrupt state. The source-only guard requires application/platform dispatcher interposition for runtime enforcement, so merged-source, repo/team-adoption, and real dispatcher-denial proofs remain separate. | Owner correction after root invented delegated design/estimates and overstated `send_message` as completion | `addons/orchestrator_session/common/{ORCHESTRATOR_BILL_OF_RIGHTS.md,orchestrator-control/root_role_guard.py}` + `tests/test_root_role_guard.py` |
| 2026-07-28 | **Duration-calibrated delegation and refill**: expanded the local control plane to schema 1.2 with fixed active-runtime buckets (`seconds`, `5m`, `10m`, `15m`, `20m`, `30m`, `45m`, `60m+`), monotonic receipt-carried estimates, separated queue/setup/active/tool-wait/external-wait/wall/first-evidence/safe-close timing, no-restart fenced rollover, early-finish evidence, a bounded 20-sample coarse family/tool/environment learner with a ≥5 completed-sample floor, queue aging/short-lane protection, heavyweight caps, and queued-setup versus active truth. Setup/create failure is an external-wrapper transition: roll back/reconcile the reservation and immediately select the next eligible task; the source CLI never claims dispatcher enforcement. The privacy-safe seed aggregate (SHA-256 `c3739bb1abff972ba6a85ecacfd9b794c6843d972b2ff90320b7eef67030585a`) has four completed observations across three families plus four censored snapshots, so it cannot enable an automatic prior. Calibration state excludes raw prompts/hashes, identifiers, titles, paths, URLs/email, PHI/private content, secrets, commands, diffs, and output. | Privacy-safe duration audit aggregate and owner scheduling/refill correction | `addons/orchestrator_session/common/{ORCHESTRATOR_BILL_OF_RIGHTS.md,docs/ORCHESTRATOR_SESSION.md,orchestrator-control}/` + `tests/test_{duration_control,orchestrator_session_contract}.py` |
| 2026-07-27 | **Cohesive browser-assistant safety module**: consolidated deterministic intent parsing, exact accessible-label link resolution, typed isolated-world messaging, ambiguity/fail-closed behavior, hostile-page scan/candidate ceilings, permission-budget pinning, and metadata-only bounded audit into one `browser-assistant/` feature directory rather than scattering helpers across extension entry points. Added a compiled content-script E2E over a synthetic hostile page (prompt-injection prose, duplicate-same-URL, different-URL ambiguity, hidden decoy, unsafe scheme, and a target beyond the scan ceiling), a real-toolbar acceptance boundary, and a durable content-script vs debugger/CDP vs native-messaging, profiles/sessions, confirmation, retry/idempotency, privacy, and Chrome Web Store guide. Product aliases, UI copy, authentication, private page content, and broad permissions were not lifted. | Auggie natural-language-navigation product branch plus the browser-assistant reference proposal | `stacks/chrome-extension/{extension/src/browser-assistant,e2e,docs}/` + `docs/{ANATOMY,LIFT-LOG}.md` |
| 2026-07-27 | **Durable Node notifier stack and failure-first tutorial**: preserved the clarity of a browser → API → background worker → notification demo while replacing ephemeral Pub/Sub work, socket-id identity, unauthenticated notify routes, unpinned/EOL dependencies, and untested multi-instance assumptions. The new profile uses Express 5 + BullMQ + Redis + Socket.IO, saved task reconciliation, explicit acknowledgement, signed loopback sessions, provider-neutral OIDC production mode, opaque tenant/user ownership, rate limits, hardened containers, real-Redis integration coverage, and a live ready → in-flight → completed → acknowledged storyboard. The tutorial now proves worker outage, missed socket recovery, retry and scale-out semantics, includes exact checkpoints/cleanup, and stops at explicit identity/Redis/deployment gates. | modernized `sample-notifier-service` v2.0.0 teaching replacement | `stacks/node-notifier/` + `examples/node-notifier.answers.json` + `docs/{ANATOMY,LIFT-LOG}.md` |
| 2026-07-27 | **Native multiline assistant composer semantics**: replaced a vertically expanding single-line control with a true macOS text area and made keyboard intent explicit: Return submits through the existing bounded send path, while Shift-Return inserts a newline and performs no Router request. Preserved default-off, loopback-only, non-persistent chat boundaries; added unit coverage for modifier routing and newline preservation plus a synthetic rebuilt-app frame and compact state/retry map. | owner acceptance feedback on the reusable native operations floater | `prototypes/operations-floater/{Sources,Tests,docs}/` + `docs/{ANATOMY,LIFT-LOG}.md` |
| 2026-07-27 | **Observable natural-language recorder host**: normalized untrusted voice narration through an explicitly named resident local model into a strict closed multi-command schema, then rendered the voice → transcript → NLP → command → validation → recorder transformations in the native UI. Added privacy-bounded mouse/key metadata echo, persisted header collapse with actual lifecycle/polling suspension, and corrected the normal window from `canJoinAllSpaces` to one user-movable macOS Space. Unknown output, hidden-window events, and module refusals stay visible and fail closed; printable keys are never reconstructed and no replay path was added. | Relative XY recorder user acceptance feedback | `prototypes/operations-floater/{Sources,Tests,docs}/` + `docs/{ANATOMY,LIFT-LOG}.md` |
| 2026-07-27 | **Dense privacy-neutral operations-dashboard web reference**: generalized a sibling dashboard into a dependency-free ten-lane static renderer using the shared `running`/`queued`/`waiting`/`ready` lifecycle. Resource, test, exposure, and verification evidence stay distinct; absent measurements render as unavailable, and committed fixtures claim no executed tests. The renderer remains one-shot and fail-closed, uses text nodes instead of HTML interpolation, adds bounded future compatibility for six optional queue progress/resource fields, collapses from five to two to one lifecycle columns, and ships executable privacy/unit coverage plus rebuilt desktop/narrow frames and a failure/rollback map. No portfolio records, private locations, telemetry, credentials, Sites configuration, or browser-to-device bridge were lifted. | genericized sibling web dashboard layout at local commit `bb50f19` | `prototypes/operations-dashboard-web/` + `docs/{ANATOMY,OPERATIONS-DASHBOARD}.md` |
| 2026-07-26 | **Privacy-split unified operations dashboard reference**: one strict versioned contract represents queue, tests, resource scheduling, and signals with per-record exposure and verification. A sandboxed native macOS floater may consume verified local-only state, while the browser renders only a deliberately produced `sanitized-remote` static snapshot. The offline publisher validates before filtering, allowlists output fields, rejects private-looking sanitized content, limits input to a regular file no larger than one megabyte, produces content-addressed immutable releases, verifies hashes before activation, and compensates back to the prior release if rollback-history commit fails. Unrun tests never become passing, unavailable telemetry stays unavailable, and estimates stay estimates. Shipped as opt-in `prototypes/` references rather than default generator output, with cross-surface, privacy, tamper, HTTP retrieval, activation, update, failure-compensation, and rollback tests plus an operator runbook. | generic operations-dashboard and native-floater prototypes | `prototypes/operations-{dashboard,dashboard-web,floater}/` + `docs/OPERATIONS-DASHBOARD.md` + landing page |
| 2026-07-26 | **kokoro_warm drift guard** (harden the vendor→reuse loop): a vendored component silently degrades into a fork the moment someone hand-edits it or the source moves on, which defeats the point of a shared standard. Added `sync-kokoro-warm.sh` (re-pull from firestarter; `--check` diffs the local copy against upstream and exits non-zero on drift; self-locating vendor dir, `KOKORO_WARM_DIR` override) and a least-privilege (`contents:read`/`issues:write`) `narration-audio-sync.yml` that runs `--check` weekly and opens **one** deduplicated tracking issue on drift — a nudge, never a merge gate. Proven live by making the sibling chrome-extension (which vendored this add-on) consume the same mechanism. | sibling chrome-extension (vendor→reuse of #24) | addons/kokoro_warm/common/scripts/ + .github/workflows/ + docs/{KOKORO_WARM,ANATOMY}.md |
| 2026-07-26 | **`kokoro_warm` narration audio standard** (new stack-agnostic add-on): one machine-readable `narration-audio.spec.json` (mono·48 kHz·-16 LUFS·-1.5 dBTP ceiling·-2.5 encode target·HP 80+limiter·AAC 96k·`af_heart`/Kokoro) that a dependency-free reader (`narration-spec.sh`, no jq/python/node) feeds to **both** a delivery masterer (`master-narration-audio.sh`: HP+limiter→48 k mono→2-pass loudnorm; copies video+captions bit-for-bit, re-encodes only audio) **and** a CI guard (`check-narration-audio.sh` + `narration-audio.yml`), so the encode target and the gate that enforces it can never drift apart. Ships the Warm Heart `render-narration.sh` (voice defaults read from the same spec) and the standard/how-to docs. Distilled from a sibling chrome-extension whose 6 help clips had drifted to mixed 48 k/96 k with plosive-popping true peaks up to -0.17 dBTP precisely because nothing enforced the standard and clips were finished ad hoc (Descript). Guard **passes** on an empty/missing clips dir so a freshly-stamped project is never red; an optional private tailnet-service tutorial reuses `docs/REMOTE-ACCESS.md` rather than duplicating it. | sibling chrome-extension (help-clip audio drift fix) | addons/kokoro_warm/ + bin/generate.py + firestarter.config.json + docs/ANATOMY.md |
| 2026-07-17 | **Stable named-tunnel runbook** (fastapi-next): `DEPLOY.md` shipped the ephemeral quick-tunnel but explicitly *punted* on a stable hostname ("set up a named tunnel … and swap the cloudflared command"). Filled that exact gap: a copy-and-fill `deploy/cloudflared/config.example.yml` (tokenized `{{ project_slug }}`/`{{ port_web }}`, ingress + catch-all 404), a `login → create → route dns → run` runbook section in `DEPLOY.md`, `make tunnel` (cloudflared in Docker, mounts the config dir), and gitignored `config.yml`/`*.json` tunnel secrets. Same free-tier, no-host-SDK model as the existing quick tunnel. supabase-flutter has the same quick-tunnel-only gap — mirroring the runbook there is a clean fast-follow. | sibling healer (`cloudflared/config.example.yml` + `make tunnel` + DEPLOY.md) | stacks/fastapi-next/deploy/ + Makefile + DEPLOY.md + template/.gitignore + docs/ANATOMY.md |
| 2026-07-17 | **`.claude/` version-control hygiene** (template): the generated `.gitignore` had no `.claude` rule at all, so a contributor's `settings.local.json` (personal overrides) or `.claude/worktrees/` scratch could be `git add`-ed by accident — the version-control complement to firestarter's existing session-isolation (the `.claude/settings.json` deny-rules + SessionStart hook it already ships). Added a targeted denylist that ignores only the personal/local state while keeping the committed shared config (`.claude/settings.json` + hooks + README) tracked. Used a targeted denylist rather than the sibling's `.claude/*` + re-include form precisely because firestarter *ships* `settings.json`/hooks, which an ignore-all would hide. | sibling litmus (`.gitignore` `.claude` block) | template/.gitignore + docs/ANATOMY.md |
| 2026-07-17 | **Build/deploy provenance at `/health`** (fastapi-next): stamp the build time + commit SHA into the backend image as trailing `ARG`/`ENV` layers (so they never bust the dep/source cache), pass them via compose `build.args` (empty locally), have `make up` set them from the host clock + `git rev-parse --short HEAD`, and surface `built_at`/`git_sha` in the `GET /health` payload. Standard, universal way to verify *which* commit is actually live after a deploy. Neutral env names (`APP_BUILD_TIME`/`APP_GIT_SHA`) so no new config token. | sibling healer (`Dockerfile`/`compose`/`make up`/`/health`) | stacks/fastapi-next/backend/Dockerfile + docker-compose.yml + Makefile + backend/app/main.py + docs/ANATOMY.md |
| 2026-07-17 | **Stale-baked-source footgun fix** (fastapi-next): the `backend-tools`/`frontend-tools` compose services baked source with **no** volume mount, so `make backend-test`/`seed`/lint after an edit silently ran the *last build's* code until a rebuild — a quiet wrong-results footgun in the exact compose firestarter ships. Mount the working tree over `/app` (backend's `pip install -e` egg-link resolves `app` from the mount; frontend keeps its baked `node_modules` via an anonymous volume), with the why baked as an inline comment. supabase-flutter already mounts, so it was unaffected. | sibling healer (`docker-compose.yml` `*-tools`) | stacks/fastapi-next/docker-compose.yml + docs/ANATOMY.md |
| 2026-07-17 | **npm lockfile cross-platform completeness guard** (fastapi-next): the `node:22` build image runs npm 10 (tolerates a `package-lock.json` missing other platforms' optional deps); a modern host runs npm 11, which rejects it with `EUSAGE` and silently breaks local `npm ci`. `make frontend-lockcheck` + a CI **Build**-job step re-resolve with `npm ci --dry-run` in a throwaway `node:24-alpine` (source mounted read-only, validated on a copy) so an incomplete lockfile fails loudly instead of on a teammate's box; wired into `precommit`. Inline WARNING carried verbatim. | sibling healer (`frontend-lockcheck` + ci step) | stacks/fastapi-next/Makefile + .github/workflows/ci.yml + docs/ANATOMY.md |
| 2026-07-15 | **Side-panel demo/QA recording capability**: capture the *real* extension side panel as video (host page + panel + visible cursor) with no login or backend — iframe the live `sidebar.html` into a staged page, seed auth/data offline via the service worker, and capture with Playwright `recordVideo` (not `ffmpeg x11grab`, which is black on a bare Xvfb). Shipped as a fill-in-the-blanks `record.template.mjs`, generic primitives (`visual_cursor_overlay.js`, `video_processor.py`), and a README of gotchas (verify-by-pixels, DNR `rules.json` modal, `/dev/shm`, site-access banner, mask-secrets-in-UI, drawtext-less ffmpeg → PNG title cards). Complements the storyboard stills harness with a moving release clip. | sibling chrome-extension `demo-recording/LEARNINGS.md` | `stacks/chrome-extension/tools/demo-recording/` + `docs/ANATOMY.md` |
| 2026-07-14 | **Evidence-backed feature handoff precept**: every handoff records acceptance, failure/recovery/rollback, exact verification, and safe inspectable evidence. Visible work adds real rebuilt-app storyboard frames and a state map, runs the full E2E rehearsal when available, captures asserted story beats/focus targets at normal test speed, and post-produces narration, captions, pacing, effects, and media validation without `slowMo` or fixed waits. Supported flows use that evidence for a 20–40s release cut; otherwise video is explicitly N/A with a still walkthrough. Non-visual work substitutes request/log/migration/build/state evidence. Wired into generated agent/human guidance, PR checklist, storyboard docs, and output ignores. | reusable sibling release workflow | `template/AGENTS.md` + `template/CLAUDE.md` + `template/CONTRIBUTING.md` + `template/docs/` + PR template |
| 2026-07-14 | Frontend dependency supply-chain hardening: upgrade the fastapi-next template from vulnerable Next.js 15.1.0 to patched 15.5.20, override its pinned vulnerable PostCSS with 8.5.10, commit `package-lock.json`, and use `npm ci` so generated local/CI/production dependency graphs are reproducible. Verified with a zero-finding production audit plus generated typecheck/build. | local template audit | `stacks/fastapi-next/frontend/` |
| 2026-07-14 | **One workstation local CA**: generated projects now require agents to reuse a host-manifested canonical Caddy CA, compare exact root fingerprints, keep CA keys service-owned, reject per-project self-signed roots/TLS bypasses, and verify durable proxy config plus rollback. Includes the macOS Login-Keychain trust runbook and `.test` naming guidance. | local-ai duplicate-Caddy-CA incident | `template/AGENTS.md` + `template/CLAUDE.md` + `template/SECURITY.md` + `template/docs/` |
| 2026-06-29 | Initial distillation: meta-layer + two stack profiles | siblings | template/ + stacks/ |
| 2026-06-29 | Self-CI: firestarter runs its own ci/review/auto-merge + lenient branch protection | (best-practice) | .github/workflows/ |
| 2026-06-29 | Sibling glean: dependabot, PR + issue templates, local ai-review.sh, postmortem template, .editorconfig, SECURITY.md, one-button Cloudflare deploy | sibling | template/ + stacks/ |
| 2026-06-29 | Storyboard manifest harness: planned-vs-implemented map rendered to docs/STORYBOARD.md with committed previews (#9) | sibling | stacks/*/storyboard/ |
| 2026-06-29 | Optional add-on mechanism + k8s Kustomize scaffold (include_k8s, default off) (#10) | sibling | addons/k8s/ + bin/generate.py |
| 2026-06-29 | gitleaks secret-scan workflow (required by auto-merge) + make target — free push-protection for private repos | security audit | template/ + stacks/ |
| 2026-06-29 | Elevated storyboarding from a merit to a stated **precept** (hard rule #6 + template AGENTS precept/invariant) | (best-practice) | AGENTS.md + template/AGENTS.md |
| 2026-06-30 | Deny-by-default RPC EXECUTE in `001_init.sql`: revoke the implicit `PUBLIC`/`anon` grant so PostgREST doesn't expose future functions until opted in (from an anon-RPC exposure pattern) | sibling | stacks/supabase-flutter/ |
| 2026-06-30 | SemVer + version-sync: canonical `/VERSION`, Keep-a-Changelog seed, per-stack `scripts/sync_version.sh` + `make version-sync` (coreutils, no SDK) | sibling | template/ + stacks/ |
| 2026-06-30 | Migration-rollback runbook (genericized): revert-as-forward-migration + emergency surgery; AGENTS pointer + OPEN_QUESTIONS backup stub | sibling | template/docs/ |
| 2026-06-30 | Flutter app now tested in CI: widget smoke test + `flutter test` wired into the Tests job and `make test`/`precommit` (was analyze-only) | sibling | stacks/supabase-flutter/ |
| 2026-06-30 | Dispatchable `deploy.yml` (universal): self-hosted `workflow_dispatch` → `make up` + `make deploy` (Cloudflare quick-tunnel, no secrets) | sibling | template/.github/workflows/ |
| 2026-06-30 | CI cost: `concurrency: cancel-in-progress` on every workflow except auto-merge (unsafe to cancel a merge); required checks keep running on every PR (no risky path-filters) | sibling | template/ + stacks/ |
| 2026-06-30 | Go-Live runbook (genericized): clean-slate run/verify + a pre-launch checklist wiring secrets, release, deploy, and backups together | sibling | template/docs/ |
| 2026-06-30 | Compliance-posture template (generalized from a sibling's compliance-posture doc): in/out-of-scope stance + risk→control→where table | sibling | template/docs/ |
| 2026-06-30 | RPC catalog convention (seed + entry template): documents PostgREST RPC grants + signatures; reinforces the deny-by-default posture | sibling | stacks/supabase-flutter/docs/ |
| 2026-06-30 | SECURITY.md: "no default/guessable secret in deploy configs" rule (fail-closed, read from env) — from sibling's demo-JWT deploy bug | sibling | template/ |
| 2026-06-30 | `include_auth` add-on (fastapi-next): passwordless OTP core (models/store/flows/delivery/router/main + smoke test), in-memory & dependency-free; OAuth flow, durable store, frontend widget deferred | sibling | addons/auth/ + bin/generate.py |
| 2026-06-30 | `include_bug_report` add-on (supabase-flutter): in-app capture → `report_bug` RPC (deny-by-default) → SQL-formatted `pull-bug-reports.sh` → GitHub issue. dep-free `dart:io` client (sibling's riverpod/supabase sheet adapted to the skeleton) | sibling | addons/bug_report/ + bin/generate.py |
| 2026-06-30 | Remote-agent-access hardened spec (doc-only, no executable): trigger-don't-connect via self-hosted `deploy.yml`, never expose `docker.sock`; broker requirements + threat model. Deliberately not a stamped default | sibling (security pass) | template/docs/ |
| 2026-06-30 | auth follow-up: durable `PostgresAuthStore` (`002_auth.sql` + `AUTH_STORE=postgres`), psycopg3 async, no new dep; skip-if-no-DB integration test drives the OTP flow through it | sibling | addons/auth/ |
| 2026-06-30 | bug_report follow-up: dependency-free screenshot capture (`ScreenshotBoundary` = keyed `RepaintBoundary` → base64 PNG), thumbnail + attach toggle; widget test captures for real | sibling | addons/bug_report/ |
| 2026-06-30 | auth follow-up: Next.js OTP sign-in widget (`auth-widget.tsx`) + `layout.tsx`/`next.config.mjs` overlays (adds `/auth/*` proxy rewrite); verified with strict tsc + next build | sibling | addons/auth/ |
| 2026-06-30 | GoTrue local bootstrap: `backend/initdb/00_create_auth_schema.sql` (CREATE SCHEMA auth + `public.schema_migrations` seed) so GoTrue doesn't crash-loop on a fresh volume; single-migration-path warning; `GOTRUE_*_AUTOCONFIRM` for local OTP | sibling | stacks/supabase-flutter/ |
| 2026-06-30 | Hermetic/integration test split (fastapi-next): `make backend-itest` + `integration` pytest marker + `tests/integration/` gated on `TEST_DATABASE_URL` (default `backend-test` stays offline); CI passes the URL so the seed runs | sibling | stacks/fastapi-next/ |
| 2026-06-30 | `include_ssrf_fetch` add-on (fastapi-next): dependency-free SSRF-guarded URL fetch (scheme + public-IP-only + redirect re-validation + size/time bounds + stdlib HTML→text), offline tests | sibling | addons/ssrf_fetch/ + bin/generate.py |
| 2026-06-30 | Least-privilege `permissions: contents: read` on every read-only workflow (commit-lint, secret-scan, storyboard, both ci.yml, splash-ci) — caps `GITHUB_TOKEN` blast radius; write workflows (ai-pr-review/auto-merge/deploy) keep their scopes | sibling | template/ + stacks/ |
| 2026-06-30 | Host-requirements onboarding runbook (genericized): host tools (Docker/git/make/gh) + "do NOT install on host" table + opt-in iOS/Android; README pointer | sibling | template/docs/ |
| 2026-07-01 | Splash Vite/webpack file-watch polling (`CHOKIDAR_USEPOLLING`/`WATCHPACK_POLLING`) as a baked gotcha — HMR fires over a Docker bind mount on macOS/Windows/WSL2 when you run a dev server (inert for the default `pnpm build`). Skipped the pnpm-store volume: cross-volume with the existing `splash_node_modules` mount, pnpm can't hardlink → net loss | sibling | stacks/supabase-flutter/ |
| 2026-07-01 | Self-audit: `GOTRUE_JWT_SECRET` was a hardcoded default (violated the #25 no-baked-secret rule) → env-driven `${JWT_SECRET:-<dev-only>}` with override/PGRST-must-match comment | security audit (#25) | stacks/supabase-flutter/ |
| 2026-07-01 | Third stack profile: `chrome-extension` (MV3 + esbuild + Vitest + host-only Playwright e2e + storyboard). First **DB-less** stack — adapts the DB-centric meta-layer (no postgres/migrate; overrides storyboard.yml). Verified: build/typecheck/vitest + e2e tsc + storyboard screenshot all pass | a sibling project | stacks/chrome-extension/ + firestarter.config.json |
| 2026-07-05 | `include_scheduled_agent` add-on (**stack-agnostic**): opt-in "cloud session" — dispatch/cron `scheduled-agent.yml` (least-privilege `contents:read`/`issues:write`) + dependency-free `agent-drop.mjs` (Node 20 `fetch`: Anthropic Messages API → opens a GitHub issue) + editable `.github/agent/prompt.md` + docs; reuses the existing `ANTHROPIC_API_KEY`, no new deps. Also generalized the add-on overlay to stamp `addons/<name>/common/` so a stack-agnostic add-on lives in one place instead of being duplicated per stack (#42) | sibling (idea-drop) | addons/scheduled_agent/common/ + bin/generate.py + firestarter.config.json |
| 2026-07-05 | Worktree best-practice framing in both `AGENTS.md` (generator + template): recommend a git worktree per concurrent session for conflict avoidance (isolated working tree + index), with the disk-space caveat + cleanup — the template already had a bare worktree line; added the *why* and the *when* | (best-practice) | AGENTS.md + template/AGENTS.md |
| 2026-07-05 | Supabase edge-functions deploy (supabase-flutter): `functions-deploy.yml` (push on `backend/supabase/functions/**` + dispatch) via `supabase/setup-cli` → `functions deploy`; distinct from the self-hosted `deploy.yml`. Generalized over sibling's copy — reads `PROJECT_REF` from `vars.SUPABASE_PROJECT_REF` (not hardcoded), auto-discovers functions, least-privilege `contents:read`, and **self-skips until configured so a fresh stamp stays green**. Ships an example `functions/hello/index.ts` so the workflow has a target (#43) | sibling (functions-deploy) | stacks/supabase-flutter/.github/workflows/ + backend/supabase/functions/ |
| 2026-07-06 | Opt-OUT auto-merge: `auto-merge-label.yml` auto-applies the `auto-merge` label to every non-draft PR on open (idempotently ensures `auto-merge`/`hold` labels; least-privilege). Completes the existing auto-merge gate — removes the "someone forgot the label" reason green PRs sit unmerged; hold with a draft or `hold` (#50) | sibling (auto-merge-label.yml) | template/.github/workflows/ |
| 2026-07-06 | Deploy-policy decision frame: `docs/DEPLOY_POLICY.md` — when a deploy is **self-authorized** (3 conditions: ample testing / snapshot verification / post-deploy check) vs. what stays owner-only (credentials, prod-DB migrations, billing). Genericized from sibling's stack-specific copy; ties condition 2 to the storyboard precept (#51) | sibling (DEPLOY_POLICY.md) | template/docs/ |
| 2026-07-06 | Response convention: agent brief instructs **every response to end with a Links footer** — repo URL (`github_owner/github_repo` tokens) + every live environment/dashboard URL — backed by a single-source-of-truth "Environments & URLs" table (local/dev/qa/staging/prod, `port_web` token). Owner best-practice from a sibling session (not yet committed there → lifted by intent, no cherry-pick) | sibling (owner directive) | template/AGENTS.md + template/CLAUDE.md |
| 2026-07-06 | Secret-rotation tool (supabase-flutter): `scripts/rotate-secrets.sh` + `make rotate-secrets` — mint a fresh `JWT_SECRET`/`DB_PASSWORD` + matching `anon`/`service_role` JWTs off the dev-only compose default → gitignored `.env.deploy` (chmod 600), JWTs minted in a throwaway `node:20-alpine` (no host SDK). Makes the no-baked-secret rule (#58/#71) actionable; `iss` tokenized to `{{ project_slug }}-selfhost`, no new config token. Also gitignore `.env.deploy` in the meta-layer (#52) | sibling (rotate-secrets.sh) | stacks/supabase-flutter/scripts/ + template/.gitignore |
| 2026-07-06 | anon-EXECUTE **guard** (supabase-flutter): `backend/security/check_anon_execute.sh` (queries `has_function_privilege('anon', …)` excluding extension-owned functions, diffs against a reviewed `anon_execute_allowlist.txt`) + `anon-execute-guard.yml` (path-gated to migrations/security, mirrors ci.yml stack-up, least-privilege `contents:read`). The **continuous backstop** to `001_init`'s deny-by-default: that only strips PUBLIC on *future* functions, so this fails CI when a new function lands anon-callable without being reviewed into the allow-list or REVOKEd. Seeded empty (stack ships no anon RPCs) so a fresh stamp stays green; distilled from sibling's #84 lockdowns (migrations 103/104) | sibling (anon-execute-guard) | stacks/supabase-flutter/backend/security/ + .github/workflows/ |
| 2026-07-06 | Prod-migration gotchas baked as inline docs: (1) `migrate.sh` records `version` as the file BASENAME (TEXT) — comment warns to keep that format consistent with any numeric CONSUMER (`MAX(version::int)`), since a mismatch throws on the first non-numeric row (real sibling incident). (2) `migration-rollback.md` gains an "applying to a hosted/managed Postgres" note: apply the same forward-only files **incrementally** (`psql "$DATABASE_URL" --single-transaction -f …` per un-applied version), never a schema **dump→restore** (not forward-only; clobbers managed-only roles/extensions/data; skips the history table). Gotcha-as-comment per the lift rules | sibling (prod-migrate footguns) | stacks/supabase-flutter/backend/scripts/migrate.sh + template/docs/migration-rollback.md |
| 2026-07-06 | Owner hand-off precept (`template/AGENTS.md`): when a session hits **owner-gated** work (prod creds / prod-DB migrations / deploys / billing — the DEPLOY_POLICY set), it must not stall or bury the ask in chat — it opens a **self-contained GitHub issue** (copy-paste runbook: exact commands + verify + done-criteria) labeled `owner-action`, so the owner (or a session holding the creds) executes it cold and the hand-offs form a filterable queue (`gh issue list --label owner-action`). Generalizes sibling's `for:mac` deploy-owner convention; complements the parallel-sessions worktree bullet (avoid clobbering) with the async-hand-off half, and gives DEPLOY_POLICY's "owner-only" list an actionable routing mechanism | sibling (for:mac hand-off) | template/AGENTS.md |
| 2026-07-07 | `.gitattributes` pins `*.sh` + `.githooks/*` to LF so host-run hooks/scripts survive a Windows checkout — a CRLF `.sh` fails with `bad interpreter: /bin/bash^M` and bricks `make hook-install`. Scoped narrowly (not a blanket `* text=auto`) to avoid renormalizing the tree (#64) | sibling (`.gitattributes`) | template/ |
| 2026-07-07 | Session-isolation guardrails: `template/.claude/settings.json` denies the blanket staging forms (`git add -A`/`--all`/`.` + `git commit -a`/`-am`/`--all`) so a session can't sweep another session's work into a commit; a git+bash **warn-only, fail-open** SessionStart hook nudges toward a worktree on a dirty tree; AGENTS/CLAUDE codify explicit-path staging. The *enforcement* half of the 2026-07-05 worktree *guidance* (#65) | sibling (`.claude/` + #102) | template/.claude/ + AGENTS.md + CLAUDE.md |
| 2026-07-07 | gitleaks **Go-RE2 no-lookahead** gotcha baked as a comment: `(?!…)`/`(?=…)`/`(?<…)`/backrefs *panic* the scanner, and since `secret-scan.yml` is a required check `auto-merge.yml` gates on, a panic reds every PR. Warn near the allowlist header + a `ci-secrets.md` note (#67) | sibling (`docs/SESSION_HANDOFF.md`) | template/.gitleaks.toml + template/docs/ci-secrets.md |
| 2026-07-07 | **Unique check-names** invariant: `auto-merge.yml` keys the verdict lookup by job *display name*, so names must be globally unique across ALL workflows or a duplicate shadows a required check (auto-merge can trust the wrong job). Comment on the `byName` step + header + AGENTS.md + ADDING-A-STACK.md (#68) | sibling (`flutter-ci.yml`) | template/.github/workflows/auto-merge.yml + AGENTS.md + docs/ADDING-A-STACK.md |
| 2026-07-07 | **Hermetic unit-tests precept** (`template/AGENTS.md`): the default suite runs with no network/secrets — construct SDK clients lazily (not at import), guard `listen()` behind a run-directly check + export the app, route on-disk state through one env-overridable dir. The universal counterpart to the fastapi-next integration split; also names the anti-pattern firestarter's own `ai-pr-review.yml`/`agent-drop.mjs` commit (load-time key) (#69) | a sibling project | template/AGENTS.md |
| 2026-07-07 | **Script-smoke**: `template/scripts/smoke.sh` `bash -n`s every `*.sh`, `sh -n`s every git hook, `py_compile`s every `*.py` (python3 optional → no host SDK forced) — a fast guard for the project's OWN shipped hooks/scripts, which the Docker test suite never exercised. Wired into each stack's **Tests** CI job + `make smoke`/`precommit` (#70) | a sibling project (`tests/run.sh`) | template/scripts/ + stacks/ |
| 2026-07-07 | **Preflight `.env` validation**: `template/scripts/verify-env.sh` reads `.env.example` as the manifest (value `__REPLACE_ME__` or `# required` ⇒ required), fails fast when a required var is unset/placeholder. Hardened line-by-line loader (never `export $(cat .env|xargs)`). `make verify-env` on all stacks + a committed all-optional `.env.example` (fresh stamp passes). Complements SECURITY.md's read-from-env rule with a *populated*-env check (#71) | a sibling project (`verify-env.sh`) | template/scripts/ + template/.env.example + stacks/ |
| 2026-07-07 | Opt-in **Xvfb e2e** for chrome-extension: `e2e/scripts/with-xvfb.sh` (idempotent boot — `pgrep` guard, don't-kill-on-exit) + `workflow_dispatch` `e2e.yml` (least-privilege `contents:read`, distinct job name so it never gates auto-merge/a fresh stamp) + container-safe Chromium flags (`--no-sandbox`/`--disable-dev-shm-usage`) in the fixture. Closes the e2e/README "need a runner with a display" TODO; headless storyboard is out of scope (#72) | a sibling project (`record.sh`) | stacks/chrome-extension/ |
| 2026-07-15 | **Symlink-safe `node_modules` ignore** (gotcha-as-comment): `.gitignore` used `node_modules/` (trailing slash), which only matches a real *directory* — a **symlinked** `node_modules` (a git worktree pointing at the main checkout's install, a common agent-worktree pattern) is not a dir to git, slips the pattern, and gets swept into a commit by `git add`. Dropped the slash in `template/.gitignore` + both `stacks/chrome-extension/{storyboard,e2e}/.gitignore` (now ignores dir, symlink, or stray file), with the why baked as an inline comment. Firestarter already had the *root-cause* guard (the `.claude/settings.json` deny of `git add -A`/`.` from #102) — this is the belt-and-suspenders half. From a sibling session that committed a `node_modules` symlink twice in worktrees. | sibling (auggie worktree sessions) | template/.gitignore + stacks/chrome-extension/ |
| 2026-07-08 | **RLS guard** — table-level sibling of the anon-execute guard (Supabase advisor `rls_disabled_in_public`): `backend/security/check_rls_enabled.sh` + `rls_disabled_allowlist.txt` (GoTrue `schema_migrations`) + path-scoped `rls-guard.yml`, all modeled on the anon-execute guard's house style (repo-root run, `SCRIPT_DIR` allow-list, count in the ✅). Fixed two latent template exposures the live guard caught: `001_init.sql`'s `GRANT SELECT … TO anon` published *every* `locations` row → RLS + `USING (is_approved)` policy; and `migrate.sh`'s `{{ migrations_table }}` bookkeeping table shipped RLS-off → deny-all RLS at bootstrap. Live-verified green on a fresh stamp (`locations`/`{{ migrations_table }}` RLS on, `schema_migrations` allow-listed, `spatial_ref_sys` extension-excluded) | sibling (#195) | stacks/supabase-flutter/ |
| 2026-07-26 | `include_secret_vault` add-on (**stack-agnostic**): cross-platform redundant secret storage that generalises the ad-hoc macOS-only `~/bin/git-crypt-key-store` into a generic tool. `secret-store`/`secret-get`/`git-crypt-key` in `scripts/*.sh` (macOS **Keychain** `security` + Linux **secret-service** `secret-tool`) **and** `scripts/*.ps1` (Windows **Credential Manager** via advapi32 `CredWrite`/`CredRead` P/Invoke — no external module — + **DPAPI**-encrypted backup) store every secret in **1Password (`op`) + the OS secret store + a locked on-disk backup**, each cross-checked by **sha256 fingerprint** and failing closed under 2 verified copies. Runtime use streams the value straight into the consumer (`secret-get --exec ENV -- cmd`) so it never hits argv/a file/a log; only the fingerprint is printed. Owner accommodation: **Windows** first-class (he runs a Windows box). Ships `docs/SECRET_VAULT.md` (concept/platform-matrix/recovery/rotation) + `docs/SECRETS.md` (the secrets **house contract**), wired into `template/AGENTS.md` (new *Secrets & credentials* section) + `template/CLAUDE.md` + `template/SECURITY.md`, plus `.secret-vault/`+`git-crypt-*.key` gitignores. bash-3.2-safe (`${arr[@]+…}`), PowerShell StrictMode-safe; verified: `bash -n` (5.x + 3.2), pwsh AST parse + sha256/CLI smoke, end-to-end store/get/tamper-refusal on a throwaway keychain, clean stamp + no token leaks + off-by-default | owner pattern (`~/bin/git-crypt-key-store`) | addons/secret_vault/ + bin/generate.py + firestarter.config.json + template/ |
