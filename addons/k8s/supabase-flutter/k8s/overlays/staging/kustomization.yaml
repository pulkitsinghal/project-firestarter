apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: {{ project_slug }}-staging
namePrefix: staging-

resources:
  - ../../base
  - namespace.yaml

commonLabels:
  environment: staging
