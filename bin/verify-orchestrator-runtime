#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec python3 \
  "$repo_root/addons/orchestrator_session/common/bin/verify-orchestrator-runtime.py" \
  --project-root "$repo_root" \
  "$@"
